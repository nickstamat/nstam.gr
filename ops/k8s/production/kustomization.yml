namespace: nstam
commonLabels:
  app: nstam

bases:
  - ../base

resources:
  - ingress.yml

images:
  - name: nstam
    newName: nickstamat/nstam
    newTag: $GITHUB_SHA

configMapGenerator:
  - name: nginx-conf
    files:
      - nginx.conf

generatorOptions:
  disableNameSuffixHash: true
